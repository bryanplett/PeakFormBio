// supabase-config.js — runtime config for Admin.html (and ClientPortal.html).
//
// Set window.SUPABASE_FN_URL to your project's Edge Functions base URL,
// which looks like:   https://<project-ref>.supabase.co/functions/v1
// You can find <project-ref> in your Supabase URL (e.g. xyzabcd.supabase.co
// → project-ref is "xyzabcd").
//
// If you don't deploy any Edge Functions yet, leave this file alone — the
// "Set password directly" advanced button will just show a friendly error.

window.SUPABASE_FN_URL = "";   // e.g. "https://xyzabcd.supabase.co/functions/v1"
